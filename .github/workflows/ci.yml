name: CI/CD

on:
  push:
    branches:
      - main

permissions:
  contents: read

env:
  APP_NAME: obstg

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Test
        run: go test ./...

      - name: Vet
        run: go vet ./...

  build:
    needs: test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build application image
        env:
          SHA: ${{ github.sha }}
        run: |
          docker build \
            -f Dockerfile \
            -t "$APP_NAME:$SHA" \
            .

      - name: Build migration image
        env:
          SHA: ${{ github.sha }}
        run: |
          docker build \
            -f Dockerfile.migrate \
            -t "$APP_NAME-migrate:$SHA" \
            .

      - name: Export images
        env:
          SHA: ${{ github.sha }}
        run: |
          docker save \
            "$APP_NAME:$SHA" \
            "$APP_NAME-migrate:$SHA" \
            | gzip > "$APP_NAME-$SHA.tar.gz"

      - name: Upload images artifact
        uses: actions/upload-artifact@v4
        with:
          name: $APP_NAME-images
          path: $APP_NAME-$SHA.tar.gz

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Download images artifact
        uses: actions/download-artifact@v4
        with:
          name: $APP_NAME-images

      - name: Configure SSH
        run: |
          mkdir -p ~/.ssh

          echo "${{ secrets.DEPLOY_SSH_KEY }}" \
            > ~/.ssh/deploy_key

          chmod 600 ~/.ssh/deploy_key

          ssh-keyscan -H \
            "${{ secrets.DEPLOY_HOST }}" \
            >> ~/.ssh/known_hosts

      - name: Upload images
        env:
          HOST: ${{ secrets.DEPLOY_HOST }}
          USER: ${{ secrets.DEPLOY_USER }}
          SHA: ${{ github.sha }}
        run: |
          scp -i ~/.ssh/deploy_key \
            "$APP_NAME-$SHA.tar.gz" \
            "$USER@$HOST:/tmp/$APP_NAME-$SHA.tar.gz"

      - name: Deploy
        env:
          HOST: ${{ secrets.DEPLOY_HOST }}
          USER: ${{ secrets.DEPLOY_USER }}
          SHA: ${{ github.sha }}
        run: |
          ssh -i ~/.ssh/deploy_key "$USER@$HOST" "
            set -e

            echo 'Loading Docker images...'
            gunzip -c /tmp/$APP_NAME-$SHA.tar.gz | docker load

            echo 'Updating application version...'
            sed -i 's/^APP_VERSION=.*/APP_VERSION=$SHA/' /opt/$APP_NAME/.env

            echo 'Starting application...'
            cd /opt/$APP_NAME
            docker compose up -d

            echo 'Cleaning up...'
            rm -f /tmp/$APP_NAME-$SHA.tar.gz

            echo 'Deployment complete'
            docker compose ps
          "
